class Todo
  def initialize(task)
    @task = task
  end

  def task
    @task
  end

  def mark_done!
  # Marks the todo as done
  # Returns nothing
  end

  def done?
  # Returns true if the todo is marked as done
  # Otherwise returns false
  end
end